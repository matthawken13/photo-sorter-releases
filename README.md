# Photo Sorter release manifest

Public version manifest polled by the BBH/WW Photo Sorter apps to check for
updates. Contains only version numbers, release notes, and links back to the
existing Google Drive `dist` folders — no source code or credentials.

Update `version.json` after each notarised release.
